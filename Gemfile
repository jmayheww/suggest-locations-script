# frozen_string_literal: true

source 'https://rubygems.org'

# gem "rails"

gem 'colorize' # This gem is used to colorize the output of the terminalgem 'dotenv' # This gem is used to load environment variables from a .env file
